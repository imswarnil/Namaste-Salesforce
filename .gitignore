node_modules
dist/
.DS_Store
*.log
npm-debug.log*
.env
.env.*

# assets/built is DELIBERATELY NOT IGNORED. Ghost serves the theme as
# uploaded and there is no build step on the server, so the compiled CSS and
# JS have to be in the repo. CI rebuilds and diffs them, which is what makes
# committing generated files safe here.
