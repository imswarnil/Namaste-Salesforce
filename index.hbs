{{!< default}}
{{!-- The default collection listing. routes.yaml points several collections
     here (see abstract/01); each gets pagination and an RSS feed for free. --}}

{{#if posts}}
  {{#foreach posts}}{{> post-card}}{{/foreach}}
{{else}}
  <p>{{t "No posts yet."}}</p>
{{/if}}

{{pagination}}
