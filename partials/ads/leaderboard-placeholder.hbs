{{!-- ads/leaderboard-placeholder.hbs — the dev-only dummy leaderboard, used by
     ads/leaderboard when no real unit is configured. It renders INSIDE that
     partial's .ns-ad wrapper, so it opens no wrapper of its own.

     Full CONTAINER width rather than a 728px box: an empty slot pinned to an
     ad-network dimension reads as a broken image, while a slot that fills the
     measure reads as inventory somebody could buy. It carries the contour
     pattern so it is obviously ours, not a blank third-party frame. --}}
<a href="/contact/" class="ns-ad__ph h-[100px] px-4 sm:h-[90px]">
  <i class="ph ph-arrow-up-right ns-ad__arrow" aria-hidden="true"></i>
  <span class="ns-ad__label"><i class="ph ph-megaphone" aria-hidden="true"></i>{{t "Book this slot for your brand"}}</span>
</a>
