{{!-- icons/steps.hbs — an ascending staircase; the training's "path / steps"
     mark. currentColor + optional class, like the rest of icons/. --}}
<svg class="{{#if class}}{{class}}{{else}}h-5 w-5{{/if}}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 20h4v-4h4v-4h4v-4h4V4"/></svg>
