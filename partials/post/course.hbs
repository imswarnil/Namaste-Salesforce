{{!-- ══════════════════════════════════════════════════════════════════════
     post/course — a course overview page: /courses/{course}/.
     ------------------------------------------------------------------------
     ⚠ STUB, and honestly so. A course page is the reading surface plus the
     curriculum — NSDS's archetype is templates/course-detail.html, and its
     behaviour is assets/js/lms.js, which is vendored and ready.

     It is not built yet because abstract/collections.md §03 is unanswered and
     §03's first question is the one that matters: how is a course NOT
     training? Both would be a list of lessons under a parent; the difference
     has to be stated before two templates exist that look the same.

     Until then a course renders as an article: the words are there, the
     furniture is not. That is the honest placeholder, and it is a DIFFERENT
     failure from the one this dispatcher just fixed — a course post used to
     fall through to {{else}} by accident. Now it lands here on purpose.
     ══════════════════════════════════════════════════════════════════════ --}}
{{> "post/article"}}
