{{!-- ══════════════════════════════════════════════════════════════════════
     post/docs — STUB.
     ------------------------------------------------------------------------
     A docs page is a reading surface with a sidebar tree on the left and an
     outline on the right. Archetype: NSDS has no single docs template; build
     it from .ns-sidenav + .ns-post__rail, and check .ns-tree first.
     Behaviour is assets/js/rail.js, which opens the group containing the
     current page.

     Blocked on the docs spec in abstract/collections/. Renders as an article
     until then.
     ══════════════════════════════════════════════════════════════════════ --}}
{{> "post/article"}}
