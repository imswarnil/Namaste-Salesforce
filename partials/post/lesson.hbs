{{!-- ══════════════════════════════════════════════════════════════════════
     post/lesson — STUB.
     ------------------------------------------------------------------------
     A lesson is a reading surface with a curriculum rail, so it does not get
     the site bar (see the chrome dispatch in default.hbs) and it is NOT a
     variant of post/article — the rail changes the page's whole column
     structure.

     The archetype to port is NS-Design-System/templates/course-player-article.html,
     and its behaviour is assets/js/lms.js. Do not start until
     abstract/collections/ has the lesson spec: the rail needs to know what a
     course IS in this content model, and guessing that is how the last
     version ended up with three copies of a lesson row.

     Rendering as an article until then is the honest placeholder — the words
     are there, the furniture is not.
     ══════════════════════════════════════════════════════════════════════ --}}
{{> "post/article"}}
