{{!-- post/prose — the article body itself, and its tag list.

     Extracted from post/body so the layout branch above it can include this
     from four places without duplicating the markup four times. --}}
<div class="gh-content ns-prose" id="post-body">
    {{content}}
</div>
