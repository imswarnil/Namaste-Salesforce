{{!-- ════════════════════════════════════════════════════════════════════════
  training/sections.hbs — THE ordered list of the training's sections.

  Section tag slugs are clean (`foundation-start`, not `train-01-…`) because a
  lesson's URL nests under its section via {primary_tag} — any prefix would leak
  into every lesson URL. Clean slugs can't encode order, so the order lives
  here, in one place. Same trade-off partials/docs/sections.hbs makes.

  Keep in sync with the /training/{section}/ routes in routes.yaml.

  Params:
    view — "card" (the /training/ grid) | "nav" (the curriculum rail)
  Anything else is passed through to training/section-item.
═══════════════════════════════════════════════════════════════════════════ --}}
{{> "training/section-item" view=view n="1" slug="foundation-start"      icon="ph-flag"}}
{{> "training/section-item" view=view n="2" slug="setup-salesforce"      icon="ph-gear-six"}}
{{> "training/section-item" view=view n="3" slug="navigating-salesforce" icon="ph-compass"}}
{{> "training/section-item" view=view n="4" slug="data-model-basics"     icon="ph-database"}}
{{> "training/section-item" view=view n="5" slug="security-basics"       icon="ph-shield-check"}}
