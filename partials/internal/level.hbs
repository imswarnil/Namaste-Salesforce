{{!-- level — the difficulty badge, from an internal tag.
     Same convention as duration: the tag's DESCRIPTION is the display text. --}}
{{~#foreach tags visibility="internal"~}}
    {{~#match slug "~^" "hash-level-"~}}{{description}}{{~/match~}}
{{~/foreach~}}
