{{!-- format — what KIND of thing a lesson is: reading, video, exercise, quiz.
     The tag's DESCRIPTION is the display text, same convention as duration
     and level, so an author adds a new format in Ghost Admin without a code
     change. --}}
{{~#foreach tags visibility="internal"~}}
    {{~#match slug "~^" "hash-format-"~}}{{description}}{{~/match~}}
{{~/foreach~}}
