<div class="widget widget-newsletter">
    <h4 class="widget-title">{{> "icons/mail"}} {{#if @custom.newsletter_heading}}{{@custom.newsletter_heading}}{{else}}{{t "Newsletter"}}{{/if}}</h4>
    <p>{{#if @custom.newsletter_text}}{{@custom.newsletter_text}}{{else}}{{t "Get new lessons and articles in your inbox. Free, no spam."}}{{/if}}</p>
    <a class="btn" href="{{@site.url}}/signup/">{{t "Subscribe"}}</a>
</div>
