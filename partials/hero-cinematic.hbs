{{!-- Hero style: CINEMATIC DARK. A full navy band with glow
fields — the enterprise-learning look. Same copy column, locally
recolored; stats become translucent tiles on the dark ground. --}}
<section class="home-hero hero-v-cinema outer relative">
    <div class="bg-grid" aria-hidden="true"></div>

    <div class="inner relative">
        <div class="home-hero-copy is-centered">
            {{> "hero-copy"}}
        </div>

        {{> "hero-stats"}}
    </div>
</section>
