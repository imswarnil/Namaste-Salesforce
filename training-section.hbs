{{!< default}}
{{!-- ══════════════════════════════════════════════════════════════════════
     training-section.hbs — the name the LIVE routes.yaml gives the training
     collection's index (`/training-sections/`).
     ------------------------------------------------------------------------
     The repo's routes.yaml names `training` for the same surface. Both must
     resolve while the two files disagree, or one of them serves index.hbs —
     the blog grid — over the curriculum. Same argument as docs/documentation;
     the layout stays in exactly one place.
     ══════════════════════════════════════════════════════════════════════ --}}
{{> "page/injection"}}
{{> "training/index"}}
