Colour-coded course difficulty as a mono status tag with a dot (green/amber/blue) — matches `Badge`'s visual language so status information always reads the same way across the product.

```jsx
<LevelBadge level="intermediate" />
```
