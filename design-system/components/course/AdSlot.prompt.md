The dashed placeholder ad/sponsor slot shown until `enable_ads`/`sponsor_*` is configured in Ghost Admin — mono uppercase label, sharp corners.

```jsx
<AdSlot />
<AdSlot label="Sponsored by Acme" height={90} />
```
