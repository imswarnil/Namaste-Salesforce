The blog listing card — same grammar as `CourseCard` (mono index chip, top accent line on hover) but with a tag pill and mono author/date/read-time meta instead of price/level.

```jsx
<BlogCard index={1} title="Why We Rebuilt the Training Roadmap" tag="Engineering" authorName="Priya Sharma" date="Jul 12" readTime="6 min" />
```
