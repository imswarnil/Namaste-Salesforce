The theme's own developer-console code block — navy tab bar with filename + copy button, flush code area, all-blue token palette (never a rainbow highlighter).

```jsx
<CodeBlock filename="apex" code={`public class Greeter {\n  // says hello\n  public static String greet() { return 'Hi'; }\n}`} />
```
