A sharp-radius text input with an optional command-palette style mono hint (`⌘K`) inset on the right — used for search bars in the courses/docs hero and catalog filters.

```jsx
<Input icon="ph-magnifying-glass" placeholder="Search courses..." hint="⌘K" />
```
