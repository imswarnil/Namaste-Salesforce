A section eyebrow rendered as a code comment (`// Getting started`) in mono, uppercase, tracked — not a pastel pill. Ties the UI's own voice back to the Apex/SOQL comments used throughout the theme's code blocks.

```jsx
<Kicker>Getting started</Kicker>
<Kicker align="center" light>On this page</Kicker>
```
