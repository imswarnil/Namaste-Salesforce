A mono, bordered status tag with a color dot — the base every course-specific badge (level, price, lesson type) is built from. Reads like a build-log status line, not a marketing pill.

```jsx
<Badge>New</Badge>
<Badge variant="success" icon="ph-check-circle">Free</Badge>
```
