{{!< default}}

<main class="error-shell">
    <div class="error-code">{{statusCode}}</div>
    <h1>{{message}}</h1>
    <p>{{t "Something went wrong — try again in a moment."}}</p>
    <a class="btn" href="{{@site.url}}">{{> "icons/home"}} {{t "Back to home"}}</a>
</main>
